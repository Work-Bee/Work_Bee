# Frontend API base (will normalize to include /api)
# During development with proxy, this can be omitted.
# For production builds, set this to your backend URL.
# Example:
# REACT_APP_API_URL=https://api.yourdomain.com/api
